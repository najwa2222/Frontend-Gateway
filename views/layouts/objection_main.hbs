<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>نظام إدارة الاعتراضات الزراعية</title>

  <meta name="description" content="نظام إدارة الاعتراضات للمزارعين - وزارة الفلاحة">
  <meta name="robots" content="noindex, nofollow">

  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css">
  <link rel="stylesheet" href="/css/fontawesome.css"> <!-- Optional: use your locally hosted FA if needed -->
  <link rel="stylesheet" href="/css/objection-styles.css">
</head>
<body>

<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/objection">نظام الاعتراضات</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link" href="/objection">الرئيسية</a>
        </li>
        
        {{!-- Display these links only if user is an admin --}}
        {{#if isAdmin}}
          <li class="nav-item">
            <a class="nav-link" href="/objection/admin/dashboard">لوحة التحكم</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/objection/admin/archive">الأرشيف</a>
          </li>
        {{/if}}
        
        {{!-- Display these links only if user is a farmer --}}
        {{#if isFarmer}}
          <li class="nav-item">
            <a class="nav-link" href="/objection/farmer/dashboard">لوحة التحكم</a>
          </li>
        {{/if}}
      </ul>
      
      <ul class="navbar-nav">
        {{!-- If user is logged in (either admin or farmer) --}}
        {{#if isLoggedIn}}
          <li class="nav-item">
            {{#if isAdmin}}
              <span class="nav-link text-warning">مرحباً، المسؤول</span>
            {{else if isFarmer}}
              <span class="nav-link text-warning">مرحباً، {{user.first_name}}</span>
            {{else}}
              <span class="nav-link text-warning">مرحباً، المستخدم</span>
            {{/if}}
          </li>
          <li class="nav-item">
            {{#if isAdmin}}
              <a class="nav-link" href="/objection/admin/logout">تسجيل الخروج</a>
            {{else}}
              <a class="nav-link" href="/objection/farmer/logout">تسجيل الخروج</a>
            {{/if}}
          </li>
        {{else}}
          {{!-- If not logged in --}}
          <li class="nav-item">
            <a class="nav-link" href="/objection/farmer/login">تسجيل الدخول</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/objection/farmer/register">تسجيل جديد</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/objection/admin/login">دخول المدير</a>
          </li>
        {{/if}}
      </ul>
    </div>
  </div>
</nav>

  <!-- Flash Messages -->
  <div class="container mt-3">
    {{#if flash.success}}
      <div class="alert alert-success alert-dismissible fade show" role="alert">
        {{#each flash.success}}<div>{{this}}</div>{{/each}}
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
      </div>
    {{/if}}

    {{#if flash.error}}
      <div class="alert alert-danger alert-dismissible fade show" role="alert">
        {{#each flash.error}}<div>{{this}}</div>{{/each}}
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
      </div>
    {{/if}}
  </div>

  <!-- Main Content -->
  <div class="container mt-4">
    {{{body}}}
  </div>

  <!-- Footer -->
  <footer class="bg-dark text-white text-center py-3 mt-5">
    © 2024 نظام إدارة الاعتراضات الزراعية. جميع الحقوق محفوظة
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script src="/js/objection-scripts.js"></script>
</body>
</html>
